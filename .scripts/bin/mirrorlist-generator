#!/bin/sh
sudo curl "https://www.archlinux.org/mirrorlist/?country=US&protocol=https&ip_version=4&use_mirror_status=on" -o "/etc/pacman.d/mirrorlist"
sleep 5
sudo sed -i 's/^#Server/Server/' /etc/pacman.d/mirrorlist
